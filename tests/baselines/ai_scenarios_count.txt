# ai-scenarios counts: passed,failed,pending,unexpectedly_passing
# ONLY THE FIRST TWO ARE GATED. ai-scenarios-check fails when passed,failed CHANGE, not when a
# scenario fails: one perf case is laptop-speed-sensitive and would redden the gate for a reason
# that is not a defect (lesson 42), while a new script error moves `failed` and is caught (159).
# pending,unexpectedly_passing are reported and never fail the gate: a PENDING scenario is one we
# EXPECT to fail, and one that passes by luck must not redden eight streams' checks. That is not
# hypothetical -- this baseline's first version recorded a 2% coin landing heads on dodge_rate.
# machine: builder0
# commit:  047a940c
# line:    scenarios: 42 passed, 2 failed, 3 pending, 0 unexpectedly passing
# reason:  combat round 10 item 5: test_artillery_stays_dug_in_on_a_moving_target re-specified (the target stops at -45,-40, in the spotter's sight on either route; after an earlier scenario its tick-2 route was empty because the map had no regions, it drove straight and parked out of sight). PASS in every order. a_scout_works_onto_a_tanks_engine_deck stays red: ORBIT cannot out-circle a 50 deg/s turret around the 8.62 m tank (combat Status item 4). base_of_fire unchanged (squad).
42,2,3,0
