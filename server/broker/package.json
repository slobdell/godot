{
  "name": "tank-squad-broker",
  "version": "0.1.0",
  "private": true,
  "description": "Tank Squad match broker: lobbies with join codes + WebSocket relay (netcode stream, N0)",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "ws": "8.21.3"
  }
}
