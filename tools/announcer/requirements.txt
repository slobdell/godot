# The real ElevenLabs run only (make announcer-generate APPROVED=1). Dry runs, the mock, and tests need nothing.
# Pinned to the version of the lead's mavlink-hud reference pipeline; SDK calls verified against it 2026-09-15.
elevenlabs==2.24.0
